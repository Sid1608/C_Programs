#include<stdio.h>
main()
{
    int ht,k_loss;
    printf("height from which balls are released");
    scanf("%d"&ht);
    printf("loss of kinetic energy");
    scanf("%d",&k_loss);
    v=(2*g*h)**0.5;
    kei=1/2*m*(v**2);
    if(k_loss>12)
        printf("the registerd loss is invalid");

    for()
    {

        ke=k_loss/100*kei;


    }
    ke=12/100*kei


}
